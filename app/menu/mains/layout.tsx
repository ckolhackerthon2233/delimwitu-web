export const metadata = {
  title: "Mains Menu - delimwitu",
  description: "Browse our main course selections",
};

export default function MainsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
