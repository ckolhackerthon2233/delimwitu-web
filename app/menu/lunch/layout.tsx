export const metadata = {
  title: "Lunch Menu - delimwitu",
  description: "Browse our lunch selections",
};

export default function LunchLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
