export const metadata = {
  title: "Food Menu - delimwitu",
  description: "Browse our breakfast and lunch selections",
};

export default function FoodLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
