export const metadata = {
  title: "Desserts Menu - delimwitu",
  description: "Browse our dessert selections",
};

export default function DessertsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
