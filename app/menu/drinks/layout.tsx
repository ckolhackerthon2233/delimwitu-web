export const metadata = {
  title: "Drinks Menu - delimwitu",
  description: "Browse our beverage selections",
};

export default function DrinksLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
